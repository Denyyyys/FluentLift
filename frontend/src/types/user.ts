export interface DeckCreatorDto {
    id: number
    name: string;
    email: string;
}

export type LoginInfo = {
    email: string;
    password: string;
}


export type SignUpInfo = {
    email: string;
    name: string;
    password: string;
}
